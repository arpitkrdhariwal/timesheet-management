[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  # Helps NextAuth + middleware agree on HTTPS cookie names on Netlify
  AUTH_TRUST_HOST = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
